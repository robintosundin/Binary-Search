/**
 * Enforce a minimum of performing 3 operations on a set of integers (an ADT).
 * @author Robin Sundin
 */
public interface IntSet {
    public void add(int element);
    public boolean contains(int element);
    public void remove(int element); 
}
